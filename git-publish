cd `dirname $0`


echo "creating new orphan branch"

git checkout --orphan tmp


echo "adding files to new branch"

git add . && git commit -m.


echo "renaming/replacing branch"

git branch -D box
git branch -m box


echo "force pushing"

git push --set-upstream origin -f box

